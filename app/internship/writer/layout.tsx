import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Writer Team Registration',
};

export default function WriterInternshipLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
